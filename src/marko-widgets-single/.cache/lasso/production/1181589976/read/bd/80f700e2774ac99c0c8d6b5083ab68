$rmod.def("/main", function(require, exports, module, __filename, __dirname) { var widget = require('./widget_one')
    .render({
        hello: 'hello'
    })
    .appendTo(document.body)
    .getWidget();
});